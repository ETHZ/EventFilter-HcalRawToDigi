process DUMP  = {

	source = PoolInputService { 
		untracked string fileName = 'Persist.root'
		untracked int32 maxEvents = -1
	}

        module dumper = HFDataFrameDumper {
        }

	path p = { dumper }
}
